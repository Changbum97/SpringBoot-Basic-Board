spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
    hibernate:
      ddl-auto: update

# 한글 깨짐 현상 방지 및 포트 변경
server:
  port: 8084
  servlet:
    encoding:
      force-response: true

file:
  dir: /root/SpringBoot-Basic-Board/src/main/resources/static/upload-images/
